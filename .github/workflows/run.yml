name: Scan GitHub Repositories and build

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  scan-and-build:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run scan repositories script
        run: .\.scripts\Write-Log.ps1
        shell: pwsh